#include <stdio.h>

int main() {
    char str1[100], str2[100];

    // Read two strings from the user
    scanf("%s %s", str1, str2);

    // Print the two strings
    printf("You entered: %s and %s\n", str1, str2);

    return 0;
}
